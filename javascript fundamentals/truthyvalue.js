" ","false",[],{},function name(params) {
    
},"0"

let userids=[]
if (userids.length==0) {
    console.log("not a value in array"); 
}

let newobject ={}
if (Object.keys(newobject).length==0) {
    console.log("empty object");
}
